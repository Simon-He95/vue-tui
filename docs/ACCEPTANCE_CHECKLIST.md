# vue-tui 验收标准检查清单

**版本**: 1.0  
**日期**: 2026-07-09  
**适用于**: Bug 修复和性能优化验收

---

## ✅ 验收标准总览

### 阻塞性标准 (必须 100% 通过)

- [ ] **所有 P0 测试用例通过**
- [ ] **现有测试套件 100% 通过**
- [ ] **API 兼容性测试通过**
- [ ] **无内存泄漏**
- [ ] **无性能回退 (>5%)**

### 非阻塞性标准 (可延后修复)

- [ ] P1/P2 测试用例通过率 > 95%
- [ ] 性能提升达到目标值
- [ ] 文档已更新

---

## 📋 Bug 修复验收清单

### Bug #1: 代理对误判为 ASCII

#### 功能正确性

- [ ] **B1.1**: Emoji 代理对检测正确 (😀 → `isAscii() = false`)
- [ ] **B1.2**: 多字节 Emoji 检测正确 (👨‍👩‍👧‍👦 → 非 ASCII)
- [ ] **B1.3**: 古文字检测正确 (𠮷 → 非 ASCII)
- [ ] **B1.4**: 纯 ASCII 正确性保持 ("Hello" → `isAscii() = true`)
- [ ] **B1.5**: 混合文本检测正确 ("Hi😀" → 非 ASCII)

#### 宽度计算

- [ ] **B1.6**: Emoji 宽度正确 (😀 → 2 cells)
- [ ] **B1.7**: CJK 宽度正确 (你好 → 4 cells)
- [ ] **B1.8**: 布局完整性测试通过

#### 性能

- [ ] **B1.9**: ASCII 检测性能回退 < 5%
- [ ] 1000 次 ASCII 检测 < 50ms

#### 边界测试

- [ ] **B1.10**: 空字符串返回 true
- [ ] **B1.11**: 单字节边界 (\x7F) 正确
- [ ] 孤立代理对不崩溃

---

### Bug #2: 多实例缓存串扰

#### 缓存隔离

- [ ] **B2.1**: 2 个实例并发渲染无串扰
- [ ] **B2.2**: 不同 widthProvider 互不影响
- [ ] **B2.3**: 实例 A 的缓存不泄露到实例 B

#### 实例生命周期

- [ ] **B2.4**: 实例销毁后缓存被清理
- [ ] **B2.8**: 创建/销毁 100 次，内存泄漏 < 10MB

#### 性能

- [ ] **B2.5**: 实例 A 的大量缓存不影响实例 B 性能
- [ ] **B2.6**: 嵌套 renderPass 栈深度正确
- [ ] **B2.7**: 10 个实例并发渲染 100% 成功

---

### Bug #3: 超长文本性能退化

#### 性能目标

- [ ] **B3.1**: 10k ASCII 字符 < 100ms ✅ **阻塞性**
- [ ] **B3.2**: 10k CJK 字符 < 100ms ✅ **阻塞性**
- [ ] **B3.3**: 100k 字符压力测试 < 500ms
- [ ] **B3.4**: 混合文本 (10k) < 150ms ✅ **阻塞性**

#### 缓存效果

- [ ] **B3.5**: 重复计算第 2 次 < 5ms
- [ ] 缓存命中率 > 60%

#### 正确性

- [ ] **B3.6**: 分段缓存宽度 = 全文宽度
- [ ] 分段边界不破坏多字节字符

#### 内存和边界

- [ ] **B3.7**: 10 个 10k 文本，缓存增长 < 5MB
- [ ] **B3.8**: ASCII/CJK/混合性能差异 < 2x
- [ ] **B3.9**: 1M 字符不崩溃

---

## 🚀 性能优化验收清单

### Opt #1: ASCII 快速路径补全

#### 性能提升

- [ ] ASCII 100 字符性能提升 > 40% (目标 50%)
- [ ] ASCII 1000 字符性能提升 > 40% (目标 50%)
- [ ] 非 ASCII 文本性能无回退

#### 准确性

- [ ] ASCII 快速路径准确率 100%
- [ ] 缓存命中率维持或提升

---

### Opt #2: 调大缓存上限

#### 缓存性能

- [ ] 缓存命中率提升到 > 60% (基线 ~45%)
- [ ] 缓存命中率提升 > 20%

#### 内存

- [ ] 内存增加 < 2MB
- [ ] 无内存泄漏

#### 性能

- [ ] 渲染性能无回退
- [ ] 缓存清理延迟 < 10ms

---

### Opt #3: 临时数组复用

#### GC 压力

- [ ] GC 次数/分钟降低 > 5% (目标 10%)
- [ ] 内存堆增长 < 5%

#### 稳定性

- [ ] 渲染性能无回退
- [ ] 并发安全性测试通过

---

## 🛡️ 破坏性变更检测清单

### API 兼容性

- [ ] `textCellWidth(text: string): number` 签名不变
- [ ] `currentTextWidthProvider()` 签名不变
- [ ] `hasTextWidthAsciiFastPath()` 签名不变
- [ ] `withTextWidthProvider<T>()` 签名不变
- [ ] `withTextRenderPass<T>()` 签名不变

### 默认行为

- [ ] 空字符串返回 0
- [ ] 单字节字符返回 1
- [ ] CJK 字符返回 2
- [ ] 错误处理不抛出异常

### 现有测试

- [ ] `pnpm test` 100% 通过 ✅ **阻塞性**
- [ ] 无新增失败用例
- [ ] 测试覆盖率不降低

### 边界行为

- [ ] 空输入不崩溃
- [ ] 极大输入 (1M) 不崩溃
- [ ] 特殊字符 (\0, \t, \n) 正确处理
- [ ] Unicode 边界 (U+10FFFF) 正确处理
- [ ] 组合字符正确处理
- [ ] 零宽字符不崩溃

### 性能回退

- [ ] 短文本 (<100 字符) 性能回退 < 5% ✅ **阻塞性**
- [ ] 中等文本 (100-1000) 性能回退 < 5% ✅ **阻塞性**
- [ ] 虚拟滚动 60fps 维持
- [ ] 1000 行渲染性能回退 < 10%
- [ ] 内存占用增加 < 10MB

---

## 📊 基准数据验收

### 必须收集的指标

- [ ] ASCII 文本宽度计算时间 (10/100/1000 字符)
- [ ] CJK 文本宽度计算时间 (10/100/1000 字符)
- [ ] 混合文本宽度计算时间
- [ ] 缓存命中率
- [ ] 虚拟滚动 FPS
- [ ] 内存占用
- [ ] GC 次数/分钟

### 基准对比

- [ ] 修复前基准数据已收集
- [ ] 修复后基准数据已收集
- [ ] 对比报告已生成

---

## 🎯 量化目标达成表

| 指标 | 基线 | 目标 | 实际 | 状态 |
|------|------|------|------|------|
| **Bug 修复** ||||
| 代理对检测准确率 | 0% | 100% | ? | ⏳ |
| 多实例并发正确性 | ❌ | ✅ | ? | ⏳ |
| 10k 字符处理时间 | >1000ms | <100ms | ? | ⏳ |
| **性能优化** ||||
| ASCII 性能提升 | 0% | +50% | ? | ⏳ |
| 缓存命中率 | 45% | 65% | ? | ⏳ |
| GC 压力降低 | 0% | -10% | ? | ⏳ |
| **兼容性** ||||
| 现有测试通过率 | 100% | 100% | ? | ⏳ |
| API 兼容性 | 100% | 100% | ? | ⏳ |
| 内存占用增加 | 0MB | <2MB | ? | ⏳ |
| 性能回退 | 0% | <5% | ? | ⏳ |

---

## 🔄 执行步骤

### 1. 前置检查

```bash
# 类型检查
pnpm run typecheck

# 代码风格
pnpm run lint

# 确保依赖最新
pnpm install
```

**检查清单**:
- [ ] TypeScript 编译通过
- [ ] ESLint 无错误
- [ ] 依赖已安装

---

### 2. 基线数据收集

```bash
# 切换到修复前的代码
git checkout main

# 运行基准测试
node scripts/benchmark-suite.ts run baseline baseline.json

# 运行完整测试套件
pnpm test --reporter=verbose > test-baseline.log
```

**检查清单**:
- [ ] `baseline.json` 已生成
- [ ] `test-baseline.log` 已生成
- [ ] 基线测试全部通过

---

### 3. Bug 修复验收

```bash
# 切换到修复分支
git checkout fix-branch

# 运行 Bug 修复测试
pnpm test test/acceptance/bug-1-surrogate-pairs.test.ts
pnpm test test/acceptance/bug-2-cache-isolation.test.ts
pnpm test test/acceptance/bug-3-long-text-perf.test.ts
```

**检查清单**:
- [ ] Bug #1 测试全部通过
- [ ] Bug #2 测试全部通过
- [ ] Bug #3 测试全部通过

---

### 4. 性能优化验收

```bash
# 运行基准测试
node scripts/benchmark-suite.ts run optimized optimized.json

# 运行性能基准 (Vitest bench)
pnpm test test/acceptance/opt-1-ascii-fast-path.bench.ts --run

# 对比基线
node scripts/benchmark-suite.ts compare baseline.json optimized.json
```

**检查清单**:
- [ ] `optimized.json` 已生成
- [ ] 性能对比报告已生成
- [ ] 性能目标达成

---

### 5. 破坏性变更检测

```bash
# 运行完整测试套件
pnpm test

# API 兼容性
pnpm test test/acceptance/api-compatibility.test.ts

# 边界测试
pnpm test test/acceptance/edge-cases.test.ts

# 性能回归
pnpm test test/acceptance/performance-regression.test.ts
```

**检查清单**:
- [ ] 完整测试套件 100% 通过 ✅ **阻塞性**
- [ ] API 兼容性测试通过
- [ ] 边界测试通过
- [ ] 性能回归测试通过

---

### 6. 最终验收

**阻塞性检查 (全部通过才能合并)**:

- [ ] 所有 P0 测试用例通过
- [ ] 现有测试套件 100% 通过
- [ ] API 兼容性 100%
- [ ] 无内存泄漏 (<10MB)
- [ ] 无性能回退 (<5%)

**量化目标**:

- [ ] 代理对检测准确率 = 100%
- [ ] 10k 字符处理 < 100ms
- [ ] ASCII 性能提升 > 40%
- [ ] 缓存命中率 > 60%

**文档**:

- [ ] CHANGELOG 已更新
- [ ] API 文档已更新 (如有变更)
- [ ] 验收报告已生成

---

## 📝 验收报告模板

```markdown
# vue-tui 优化验收报告

## 执行信息
- 日期: YYYY-MM-DD
- 执行人: @username
- 分支: fix-branch
- Commit: abc123

## Bug 修复验收
### Bug #1: 代理对误判
- 状态: ✅ 通过 / ❌ 失败
- 测试通过率: X/Y
- 备注: ...

### Bug #2: 多实例缓存串扰
- 状态: ✅ 通过 / ❌ 失败
- 测试通过率: X/Y
- 备注: ...

### Bug #3: 超长文本性能
- 状态: ✅ 通过 / ❌ 失败
- 10k 字符时间: Xms (目标 <100ms)
- 备注: ...

## 性能优化验收
### ASCII 快速路径
- 100 字符提升: +X% (目标 >50%)
- 1000 字符提升: +X% (目标 >50%)

### 缓存优化
- 命中率: X% (目标 >65%)
- 内存增加: XMB (目标 <2MB)

## 破坏性变更检测
- 现有测试通过率: X%
- API 兼容性: ✅ / ❌
- 性能回退: X%

## 最终决策
- [ ] ✅ 通过验收，可以合并
- [ ] ❌ 未通过验收，需要修复: ...
```

---

## 🚨 失败处理流程

### 如果阻塞性标准未通过

1. **停止合并流程**
2. **记录失败原因**
3. **创建修复任务**
4. **重新执行验收**

### 如果非阻塞性标准未通过

1. **记录为已知问题**
2. **评估影响范围**
3. **决定是否延后修复**
4. **更新文档说明**

---

**文档状态**: ✅ 完成  
**最后更新**: 2026-07-09  
**维护者**: @simon_he
