/**
 * Ambient declarations required when type-checking against the vue-tui source
 * tree (via tsconfig paths). `__VUE_TUI_PERF_INSTRUMENTATION__` is a build-time
 * constant the main package uses to tree-shake its perf instrumentation.
 */
declare const __VUE_TUI_PERF_INSTRUMENTATION__: boolean;
